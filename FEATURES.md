# 📚 Портал для Студентов и Преподавателей

## 🎯 Функционал, который был добавлен

### Для Преподавателей 👨‍🏫

1. **Админ-Панель Преподавателя** (`/teacher`)
   - ✅ Три вкладки: ДЗ, Оценки, Студенты
   - ✅ Выбор группы для управления
   - ✅ **Список всех студентов группы** (зарегистрированные и незарегистрированные)
   - ✅ Статус регистрации каждого студента

2. **Управление Домашними Заданиями**
   - ✅ Создание ДЗ по предметам для групп
   - ✅ Просмотр актуальных заданий
   - ✅ Удаление устаревших ДЗ
   - ✅ Сохранение информации о преподавателе и сроке

3. **Выставление Оценок**
   - ✅ Выставление баллов студентам по предметам
   - ✅ История всех оценок в группе
   - ✅ Цветовая система: зеленый (80+), синий (60-79), красный (<60)
   - ✅ Отображение даты выставления оценки

4. **Управление Группой**
   - ✅ Просмотр всех студентов группы в одном месте
   - ✅ Индикатор статуса (зарегистрирован/не зарегистрирован)
   - ✅ ID/ИИН каждого студента

### Для Студентов 👨‍🎓

1. **Кабинет Студента** (`/dashboard`)
   - ✅ **Актуальный список ДЗ** по группе
   - ✅ Предмет, описание и срок для каждого ДЗ
   - ✅ Имя преподавателя

2. **Просмотр Оценок**
   - ✅ **Все оценки студента** по предметам
   - ✅ Средний балл (автоматический расчет)
   - ✅ Цветовая система оценок
   - ✅ История оценок в обратном хронологическом порядке

3. **Статистика**
   - ✅ Количество активных заданий
   - ✅ Количество полученных оценок
   - ✅ Информация о группе

## 🏗️ Структура БД (требуемые таблицы)

```sql
-- Таблица для преподавателей
CREATE TABLE teacher_profiles (
  id BIGSERIAL PRIMARY KEY,
  login TEXT NOT NULL UNIQUE,
  password TEXT NOT NULL,
  full_name TEXT NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Таблица для студентов (коды регистрации)
CREATE TABLE registration_codes (
  id BIGSERIAL PRIMARY KEY,
  code TEXT NOT NULL UNIQUE,      -- ИИН студента
  full_name TEXT NOT NULL,
  group_name TEXT NOT NULL,
  password TEXT,
  is_password_changed BOOLEAN DEFAULT FALSE,
  used_by TEXT,                   -- ID студента после регистрации
  created_at TIMESTAMP DEFAULT NOW()
);

-- Таблица для домашних заданий
CREATE TABLE homeworks (
  id BIGSERIAL PRIMARY KEY,
  group_name TEXT NOT NULL,
  subject TEXT NOT NULL,
  task_text TEXT NOT NULL,
  teacher_name TEXT,
  deadline DATE,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Таблица для оценок
CREATE TABLE grades (
  id BIGSERIAL PRIMARY KEY,
  student_id TEXT NOT NULL,
  group_name TEXT NOT NULL,
  subject TEXT NOT NULL,
  grade_value INTEGER NOT NULL,
  teacher_name TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
```

## 📁 Новые Файлы

### Утилиты (`lib/api/dataService.js`)

- `fetchHomeworksByGroup()` - загрузка ДЗ группы
- `createHomework()` - создание ДЗ
- `deleteHomework()` - удаление ДЗ
- `fetchGradesByStudentId()` - загрузка оценок студента
- `fetchGradesByGroup()` - загрузка оценок группы
- `createGrade()` - создание оценки
- `fetchStudentsByGroup()` - загрузка студентов группы
- `calculateAverageGrade()` - расчет среднего балла
- `getGradeColor()` - определение цвета оценки

### Хуки (`lib/hooks/useAuth.js`)

- `useAuthCheck()` - проверка аутентификации
- `useUserData()` - получение данных пользователя
- `logout()` - выход из системы

## 🔄 Обновленные Файлы

### 1. `/app/teacher/page.js`

- Добавлена система вкладок (Homework, Grades, Students)
- Вкладка "Домашние задания" - управление ДЗ
- Вкладка "Оценки" - выставление и просмотр оценок
- Вкладка "Студенты" - список всех студентов группы
- Улучшенное отображение данных
- Интеграция с утилитами

### 2. `/app/dashboard/page.js`

- Добавлены реальные ДЗ из БД
- Добавлены реальные оценки студента
- Автоматический расчет среднего балла
- Цветовая система для оценок
- Статистика по заданиям и оценкам

## 🚀 Как Использовать

### Для Преподавателей:

1. Перейти на `/teacher`
2. Выбрать группу
3. Перейти на вкладку "Домашние задания"
4. Заполнить предмет и описание задания
5. Нажать "Опубликовать ДЗ"
6. Выставить оценки через вкладку "Оценки"
7. Просмотреть студентов через вкладку "Студенты"

### Для Студентов:

1. Перейти на `/dashboard`
2. Просмотреть актуальные ДЗ
3. Просмотреть свои оценки
4. Увидеть средний балл

## 🛠️ Технические Детали

- **Framework**: Next.js 16+
- **ORM/Database**: Supabase (PostgreSQL)
- **UI**: Tailwind CSS + Lucide Icons
- **Состояние**: React Hooks (useState, useEffect)

## 📝 Примечания

- ДЗ распределяются по группам, видны всем студентам этой группы
- Оценки привязаны к студентам через `student_id` (ИИН из registration_codes)
- Все данные синхронизируются в реальном времени через Supabase
- Система автоматически загружает данные при загрузке страницы

## 🔒 Безопасность

- Проверка роли пользователя перед доступом
- Преподаватели видят только своих студентов
- Студенты видят только свои оценки
- Logout очищает localStorage
